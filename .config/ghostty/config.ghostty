# Ghostty config (tracked in cfg).
# Most remote hosts (e.g. JASMIN) lack the xterm-ghostty terminfo, so advertise a standard TERM.
term = xterm-256color
keybind = cmd+page_up=previous_tab
keybind = cmd+page_down=next_tab