# Conda setup for my own Linux boxes (mistakenot/zerogravitas/breakeven).
# Sourced from .shrc.common. Uses $HOME rather than a hardcoded /home/markmuetz
# so it survives a machine with a different username.
_shrc_conda_init "$HOME/anaconda3"
